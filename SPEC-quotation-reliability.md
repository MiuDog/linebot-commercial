# SPEC：低 token 報價穩定性

狀態：使用者於 2026-09-15 授權開發、推送與本地 Docker 部署；環境設定與憑證由使用者管理。

## 驗收範圍

1. 報價使用 strict JSON Schema 傳輸契約，欄位相容 required／nullable 規則，保留業務驗證。供應商不支援時明確回報，不靜默降級。
2. 只傳所選格式的啟用品項；未選格式或 DYNAMIC 格式不傳固定目錄。只解析本輪 patch，不生成價格或金額。
3. 拒絕、截斷、格式與連線錯誤可區分；語法／契約修復最多一次，拒絕與驗證憑證失敗不重試。
4. 提供固定欄位 CSV 報價輸入，繞過 AI 抽取，仍沿用草稿驗證、預覽、確認及產檔流程；不得直接建立未確認報價。
5. 回歸案例涵蓋五種格式、品項候選、null patch、錯誤與 CSV；不可宣稱未量測的真實模型成功率。
6. 兩個專案通過 Maven 與 Docker 建置、容器健康檢查。LINE 外網驗收需使用者設定有效憑證、公開網址及 Tunnel。

## 分段交付

- 候選裁切與 strict 傳輸契約。
- 分類錯誤與有限修復。
- CSV 草稿入口與既有確認流程整合。
- 完整測試、部署修復、文件更新與兩個專案推送。

## 邊界

不修改既有計價規則，不由模型補造數量或價格，不將 Secret／公司資產提交 Git。OCR 持久快取與正式模型多次抽樣評估須以實測資料決定後續調整，部署結果須分開記錄容器健康與 LINE 端到端驗收。
