apiVersion: v1
kind: Service
metadata:
  name: linebot-commercial
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: linebot-commercial
  ports:
    - name: http
      port: 80
      targetPort: http

